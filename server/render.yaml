services:
  - type: web
    name: strapi
    env: node
    plan: starter
    buildCommand: yarn install && yarn build
    startCommand: rsync -a public/ /d/sqlite_data/ && yarn start
    healthCheckPath: /_health
    autoDeploy: false
    disk:
      name: strapi-data
      mountPath: /d
      sizeGB: 1
    envVars:
      - key: NODE_VERSION
        value: ~20.11.0
      - key: NODE_ENV
        value: production
      - key: DATABASE_FILENAME
        value: /d/sqlite_data/strapi.db
      - key: JWT_SECRET
        generateValue: true
      - key: ADMIN_JWT_SECRET
        generateValue: true
      - key: APP_KEYS
        generateValue: true
